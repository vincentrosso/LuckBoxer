# LuckBoxer

Monorepo containing:

- `Playing-Cards-Object-Detection/`: training + utilities (copied from TeogopK/Playing-Cards-Object-Detection-based work)
- `mobile_app/`: Flutter+iOS real-time card detection app scaffold

## iOS App

From `mobile_app/card_detector`:

```bash
flutter run -d "iPhone"
```
